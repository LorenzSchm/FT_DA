import React from "react";
import { Text, View } from "react-native";

const RegisterCardStepTwo = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Step Two</Text>
    </View>
  );
};

export default RegisterCardStepTwo;
