services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/app

  frontend:
    build: ./webapp
    ports:
      - "3000:3000"
    volumes:
      - ./webapp:/app
